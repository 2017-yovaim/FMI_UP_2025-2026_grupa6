#  Задачи върху Масиви и функции  

**Масиви като аргументи на функции:**
1. Когато предавате масив на функция се работи върху самия масив, а не върху негово копие. 
Това означава, че промените в масива вътре в функцията ще се отразят върху оригиналния масив. 
2. C++ не съхранява размера на масива по време на изпълнение, така че е важно да предавате размера на масива в отделен параметър.
   
## Пример:
```cpp
void modifyArray(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        arr[i] *= 2; // Удвояваме всеки елемент в масива
    }
}

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    modifyArray(arr, 5); 
}


```

## Задача 1

**Условие:**  
Даден е масив с n цели числа. 
Да се смени знакът на всеки елемент според това кои елементи са повече - положителните или отрицателните. 
Т.е., ако имаме 5 положителни и 4 отрицателни числа, трябва на отрицателните да се обърне знака.
(Ако + и - са еднакъв брой - всички да станат положителни) .


## Пример

**Вход 1:**
<pre>-3 1 -2 4 -5 6</pre>
**Изход:**
<pre>Пред преобразуването: -3 1 -2 4 -5 6 
След преобразуването: 3 1 2 4 5 6 
</pre>


### 💻 Решение 

```cpp
#include <iostream>

void changeSigns(int arr[], int n) {
    int positiveCount = 0;
    int negativeCount = 0;

    // Броим положителните и отрицателните числа
    for (int i = 0; i < n; i++) {
        if (arr[i] > 0) {
            positiveCount++;
        } else if (arr[i] < 0) {
            negativeCount++;
        }
    }

    // Решаваме кой тип на числа трябва да променим
    if (positiveCount > negativeCount) {
        // Ако положителните са повече, променяме знака на отрицателните
        for (int i = 0; i < n; i++) {
            if (arr[i] < 0) {
                arr[i] = -arr[i]; // Променяме знака на отрицателните числа
            }
        }
    } else if (negativeCount > positiveCount) {
        // Ако отрицателните са повече, променяме знака на положителните
        for (int i = 0; i < n; i++) {
            if (arr[i] > 0) {
                arr[i] = -arr[i]; // Променяме знака на положителните числа
            }
        }
    } else {
        // Ако броят на положителните и отрицателните е равен
        // Правим всички числа положителни
        for (int i = 0; i < n; i++) {
            if (arr[i] < 0) {
                arr[i] = -arr[i]; // Променяме знака на отрицателните
            }
        }
    }
}

int main() {
    // Статичен масив
    int arr[] = {-3, 1, -2, 4, -5, 6};
    int n = sizeof(arr) / sizeof(arr[0]); // Изчисляваме размера на масива

    std::cout << "Пред преобразуването: ";
    for (int i = 0; i < n; i++) {
        std::cout << arr[i] << " ";
    }
    std::cout << std::endl;

    changeSigns(arr, n);

    std::cout << "След преобразуването: ";
    for (int i = 0; i < n; i++) {
        std::cout << arr[i] << " ";
    }
    std::cout << std::endl;

    return 0;
}
```


## Задача 2 

**Условие:**  
Да се състави функция, която извежда на екрана, в нарастваща последователност (без повторения), първите k елемента на float масив.

## Пример

**Вход 1:**
<pre> k = 5</pre>
**Изход:**
<pre> Първите 5 елемента във възходящ ред (без повторения):
1.5 3.7 5.1 12.3 </pre>


### 💻 Решение 

```cpp
#include <iostream>
using namespace std;

// Функция за сортиране на първите k елемента (метод на мехурчето)
void sortFirstK(float a[], int k) {
    for (int i = 0; i < k - 1; i++) {
        for (int j = 0; j < k - i - 1; j++) {
            if (a[j] > a[j + 1]) {
                float temp = a[j];
                a[j] = a[j + 1];
                a[j + 1] = temp;
            }
        }
    }
}

// Функция за извеждане на първите k елемента, без повторения
void printFirstKUnique(float a[], int n, int k) {
    if (k > n) k = n; 

    sortFirstK(a, k);

    cout << "Първите " << k << " елемента във възходящ ред (без повторения):\n";

    // Извеждаме първия елемент
    if (k > 0) {
        cout << a[0] << " ";
    }

    // Извеждаме само различните
    for (int i = 1; i < k; i++) {
        if (a[i] != a[i - 1]) {
            cout << a[i] << " ";
        }
    }

    cout << endl;
}

int main() {
    float arr[] = {1.5f, 12.3f, 3.7f, 5.1f, 3.7f, 8.0f};
    int n = sizeof(arr) / sizeof(arr[0]);

    int k;
    cout << "Въведете k: ";
    cin >> k;

    printFirstKUnique(arr, n, k);

    return 0;
}


```


## Задача 3 

**Условие:**  
Да се напише функция, която получава едномерен масив от числа с плаваща запетая, цяло число k, обозначаващо колко от елементите на масива ще се проверяват.
Функцията трябва да върне true, ако първите k елемента на масива формират строго нарастваща редица, 
тоест всеки елемент е по-голям от предишния:
arr[0] < arr[1] < arr[2] < ... < arr[k-1]
Ако поне едно от тези сравнения не е изпълнено (например е равно или по-малко), функцията трябва да върне false.


---

## Пример

**Изход:**
<pre> 0Първите 4 елемента НЕ формират строго нарастваща редица. </pre>

**Обяснение** 
(Първо се отпечатва 0, защото имаме cout << isStrictlyIncreasing(arr, k);
а 0 = false)

### 💻 Решение 

```cpp
#include <iostream>

bool isStrictlyIncreasing(float arr[], int k) {
    for (int i = 0; i < k - 1; i++) {
        if (arr[i] >= arr[i + 1]) {
            return false; 
        }
    }
    return true;  
}

int main() {
    float arr[] = {1.5f, 12.3f, 3.7f, 5.1f, 8.0f};
    int k = 4; 

	
	std::cout << isStrictlyIncreasing(arr, k);

    if (isStrictlyIncreasing(arr, k)) {
        std::cout << "Първите " << k << " елемента формират строго нарастваща редица.\n";
    } else {
        std::cout << "Първите " << k << " елемента НЕ формират строго нарастваща редица.\n";
    }

    return 0;
}


```


## Още задачи от семинарното упражнение за основни действия с едномерни масиви:

- да се въведе масив с предварително заявен брой елементи
- да се въведе масив от положителни числа, въвеждането да спира при първа срещната нула (тя да не влиза в масива)
- копиране на стойностите от един масив в друг
- да се пресметне сумата на всички елементи
- да се провери дали съществува отрицателен елемент
- да се провери дали всички елементи са неотрицателни
- да се намери позицията, на която се намира дадена стойност (или -1, ако не се съдържа в масива)
- да се изтрие елемент от масив по зададен индекс (елементите след него да се избутат наляво)
- да се вмъкне елемент по зададен индекс
- да се намери стойността и/или индекса на минималния елемент
- да се сортира масива

  
