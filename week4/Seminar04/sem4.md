#  Задачи за функции 
## Задача 1

**Условие:**  
Да се състави функция на с++, която по цяло число n определя дали в двоичното му представяне няма повтарящи се съседни битове.

**Обяснение**
Функцията no_consecutive_bits(int n) проверява дали числото съдържа повтарящи се съседни битове.
Операцията (n >> 1) премества всички битове на n с една позиция надясно.
Операцията n & (n >> 1) връща число, в което ще имаме ненулеви битове на тези позиции, където и двата бита (в n и в неговото изместване) са 1.
Ако резултатът от n & (n >> 1) е 0, означава, че няма повтарящи се съседни битове.


## Пример

**Вход 1:**
<pre>За вход n = 5 (което е 101 в двоично представяне) </pre>
**Изход:**
<pre>Няма повтарящи се съседни битове.</pre>

**Вход 2:**
<pre>За вход n = 6 (което е 110 в двоично представяне)</pre>
**Изход:**
<pre>Има повтарящи се съседни битове.</pre>



### 💻 Решение 

```cpp
#include <iostream>

bool no_consecutive_bits(int n) {
    // Използваме операцията shift и сравнение
    return (n & (n >> 1)) == 0;
}

int main() {
    int n;
    std::cout << "Въведете цяло число: ";
    std::cin >> n;

    if (no_consecutive_bits(n)) {
        std::cout << "Няма повтарящи се съседни битове.\n";
    } else {
        std::cout << "Има повтарящи се съседни битове.\n";
    }

    return 0;
}
```


## Задача 2 

**Условие:**  
Да се напише програма, която сравнява числа с плаваща запетая.


### 💻 Решение 

```cpp
#include <iostream>
#include <cmath> // За std::abs

bool areEqual(double a, double b, double epsilon = 1e-9) {
    return std::abs(a - b) < epsilon; // Проверяваме дали разликата е по-малка от epsilon
}

int main() {
    double a = 0.1;
    double b = 0.1;

    if (areEqual(a, b)) {
        std::cout << "Числата са равни!" << std::endl;
    } else {
        std::cout << "Числата не са равни!" << std::endl;
    }

    return 0;
}

```


## Задача 3 

**Условие:**  
Напишете програма, която приема цяло число от потребителя, което трябва да бъде в даден интервал. 
Програмата трябва да гарантира, че въведеното число е валидно и попада в зададения интервал, 
а ако не, потребителят трябва да бъде помолен да въведе число отново, докато не подаде валидно. 

---

## Пример

**Вход 1:**
<pre>Моля, въведете число между 0 и 9: 21</pre>
**Изход:**
<pre>Моля, въведете число между 0 и 9: </pre>

**Вход 2:**
<pre>Моля, въведете число между 0 и 9: 5</pre>
**Изход:**
<pre>5</pre>


### 💻 Решение 

```cpp
#include <iostream>

int askUser(const int from, const int to) {
    int number;
    
    // Започваме цикъл с do-while, за да гарантираме поне едно въвеждане
    do {
        std::cout << "Моля, въведете число между " << from << " и " << to << ": ";
        std::cin >> number;
        
       } while (number < from || number > to); // Проверяваме дали входът е валиден и дали числото е в интервала

    return number; // Връщаме валидното число
}

int main() {
    const int MIN = 0, MAX = 9;
    int a = askUser(MIN, MAX);
    
    std::cout << a ;
}


```


## Задача 4 

**Условие:**  
Да се състави функция с 3 float параметъра, която осъществява в тях
(в последователността на цитирането им) сортировка по техните нарастващи стойности. 

---

## Пример

**Вход:**
<pre> Въведете три числа: 44 1 5 </pre>
**Изход:**
<pre> Числата в нарастващ ред: 1 5 44 </pre>


### 💻 Решение 

```cpp
#include <iostream>
using namespace std;

// Функция, която сортира три числа по нарастващ ред
void sortThreeFloats(float &a, float &b, float &c) {

    if (a > b) {
        float temp = a;
        a = b;
        b = temp;
    }

    if (b > c) {
        float temp = b;
        b = c;
        c = temp;
    }

    if (a > b) {
        float temp = a;
        a = b;
        b = temp;
    }
}

int main() {
    float x, y, z;
    
    cout << "Въведете три числа: ";
    cin >> x >> y >> z;

    sortThreeFloats(x, y, z);

    cout << "Числата в нарастващ ред: " << x << " " << y << " " << z << endl;

    return 0;
}



```
